# Artefacts de compilation LaTeX (régénérés par latexmk -pdf main.tex)
# latexmk les écrit désormais dans build/ (voir .latexmkrc) ; on ignore le dossier.
build/
*.aux
*.fls
*.fdb_latexmk
*.log
*.out
*.toc
*.synctex.gz
latexmk.log
# Le PDF est versionné (livrable) ; les artefacts ci-dessus non.
